<div id="gitalk-container"></div>

<script>
  const gitalk = new Gitalk({
    clientID: "<%= theme.Comment.Id %>",
    clientSecret: "<%= theme.Comment.Secret %>",
    repo: "<%= theme.Comment.Repo %>",      // The repository of store comments,
    owner: "<%= theme.Comment.Owner %>",
    admin: ["<%= theme.Comment.Admin %>"],
    language: "zh-CN",
    id: "<%= page.date %>",      // Ensure uniqueness and length less than 50
    distractionFreeMode: false  // Facebook-like distraction free mode
  })
  gitalk.render('gitalk-container')
</script>